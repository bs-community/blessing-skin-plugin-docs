language: node_js
node_js: 8

script: yarn build

deploy:
  skip_cleanup: true
  provider: pages
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
  local-dir: .vuepress/dist
  email: gplane@protonmail.com
  name: pify
